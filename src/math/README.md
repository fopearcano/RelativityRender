# `src/math/` — Math Library

**Layer:** L1. **Milestone:** M3. **Status:** not started.

Pure math leaf: `Vec2/3/4`, `Mat3/4`, `Quat`, `Ray`, `AABB`, transforms,
sampling primitives, color spaces, GPU-friendly inline implementations
(host- and device-callable).

**Math depends on nothing.** It is the leaf of the dependency graph.

See `docs/MODULE_MAP.md` for the authoritative contract.
