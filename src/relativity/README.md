# `src/relativity/` — Relativistic perception math

**Status (relativity-core-v1, day-1): scaffold only — no code yet.**

The prototype's RR_HD `RelativityMath` (Lorentz boost, aberration,
Doppler) and `RelativityParams` POD are clean and survive into the
rewrite per `docs/REUSE_PLAN.md`. They come back in the slice that
introduces a relativistic kernel — the day-1 gradient render is
non-relativistic.
